---
description: Backend Service — 1 operação por arquivo, classe curta, orquestra Action ou Repository
globs: backend/src/modules/**/services/**/*
alwaysApply: false
---

# Backend — Service

## Caminho

`backend/src/modules/<dominio>/services/<entidade>/<entidade>_<op>.service.ts`

## Obrigatório

- Classe curta: `ListService`, `GetService`, `CreateService`, `UpdateService`, `DeleteService`, `GetByPhoneService`.
- Fluxo principal em `execute(ctx, ...)`.
- Parâmetro tipado `<entidade>Schema` — **proibido** `data`.
- Update: `execute(ctx, customerId, customerSchema)`.
- Unicidade: chamar `GetBy<Uk>Service` antes de create/update.
- Com efeito colateral → delegar a `CreateAction` (etc.).
- Sem efeito → chamar `CreateRepository` (etc.) direto.
- Não importa `@prisma/client` nem Express.

## Proibido

- Prefixo da entidade na classe (`CustomerCreateService` ❌).
- SQL / Prisma Client.
- Controller / rotas.
- Action desnecessária em CRUD puro.

## Exemplo

```ts
export class CreateService {
  constructor(
    private readonly getByPhone: GetByPhoneService,
    private readonly createAction: CreateAction,
  ) {}
  async execute(ctx: RequestContext, customerSchema: CustomerCreateSchema) { /* ... */ }
}
```

Norma: `docs/16`, `docs/05`.
