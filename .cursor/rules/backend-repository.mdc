---
description: Backend Repository — Prisma + TenantPrisma, 1 operação por arquivo
globs: backend/src/modules/**/repositories/**/*
alwaysApply: false
---

# Backend — Repository

## Caminho

`backend/src/modules/<dominio>/repositories/<entidade>/<entidade>_<op>.repository.ts`

## Obrigatório

- Classe curta: `ListRepository`, `GetRepository`, `CreateRepository`, `UpdateRepository`, `DeleteRepository`, `GetByPhoneRepository`.
- Método `execute(...)`; acesso via `TenantPrisma` / `runInTenantContext`.
- Um arquivo = uma operação.
- Mapper row ↔ entidade de domínio em `repositories/<entidade>/mappers/`.
- Update: `execute(ctx, customerId, customerSchema)` ou entidade de domínio — PK explícita.
- Traduzir erros PG conhecidos (ex.: `23P01` → `SlotUnavailableError`).

## Proibido

- Prefixo da entidade na classe (`CustomerCreateRepository` ❌).
- Parâmetro genérico `data`.
- Orquestração entre repositories / publicação de eventos (isso é Action/Service).
- Importar Express / controllers.

## Exemplo

```ts
// customer_create.repository.ts
export class CreateRepository {
  constructor(private readonly db: TenantPrisma) {}
  async execute(ctx: RequestContext, customer: Customer): Promise<void> { /* ... */ }
}
```

Norma: `docs/16`, `docs/06-multi-tenancy.md`, ADR-0004.
